[pytest]
# Directorio raíz de pruebas
testpaths = tests

# Patrón de archivos de test
python_files = test_*.py

# Modo asyncio para pruebas async futuras
asyncio_mode = auto

# Opciones por defecto
addopts = -v --tb=short
